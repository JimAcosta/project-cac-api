services:
  - type: web
    name: project-cac-api
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn src.app:app"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: bd_alumnos
          property: connectionString
    autoDeploy: true
